using Terraria;
using Terraria.ModLoader;
using Terraria.ID;

// This idea has been put on hold. 
// There is little to no point in implementing this, other than to ease boss farming
// It's not a really funny idea, and the code itself would just require a shit load of
// trials to find frameY and frameX values of the boss trophies. 

namespace tgmod.Tiles
{
    class TrophyAsBanner : GlobalTile
    {
        /*
        public override void NearbyEffects(int i, int j, int type, bool closer)
        {
            
            if (type == TileID.Painting3X3)
            {
            }
            base.NearbyEffects(i, j, type, closer);
        }
        */
    }
}
